# Quorum Runner Canary

Disposable repository for Quorum task #205 credentialed lifecycle canaries.

The checked-in fixtures are intentionally tiny so the canary measures orchestration,
provider continuation, review, checks, and merge rather than application complexity.
